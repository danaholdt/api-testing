doctype html
html(lang="en")
    head
        title Gettings Started with HS OAuth
    body
        h1 Getting Started
        if token
            p You're authenticated using this token: #{token}
            p Your account has these contacts:  
                ul
                    each contact in contacts   
                        li #{contact.properties.firstname.value} #{contact.properties.lastname.value}
        else    
            p You're not authenticated
            p Plz install me #[a(href=`${authUrl}`) here!]